{
  "name": "stream-dom-renderer",
  "version": "1.0.0",
  "author": "Liuying <liuyingyyx@dingtalk.com>",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/implement-them/stream-dom-renderer.git"
  },
  "keywords": [
    "richtext",
    "dom",
    "stream"
  ],
  "bugs": {
    "url": "https://github.com/implement-them/stream-dom-renderer/issues"
  },
  "license": "MIT",
  "main": "./stream-dom-renderer.umd.cjs",
  "module": "./stream-dom-renderer.js",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "tsc && vite build",
    "postbuild": "node ./scripts/postbuild.js",
    "publish:alpha": "cd dist && npm publish --tag alpha && cd ..",
    "publish": "cd dist && npm publish && cd ..",
    "start": "vite dev"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.6",
    "@vitest/browser": "^1.1.0",
    "@vitest/coverage-istanbul": "^1.1.0",
    "jsdom": "^23.0.1",
    "lit": "^3.1.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vitest": "^1.1.0",
    "webdriverio": "^8.27.0"
  }
}
