import { DomReceiver, IDomCommands, IDomCommandKey, IDomCommand, IDomCommandPayload, IDomCommandValue } from './DomReceiver';

export const Receivers = {
  DomReceiver,
};

export type {
  IDomCommands, IDomCommandKey, IDomCommand, IDomCommandPayload, IDomCommandValue,
};
