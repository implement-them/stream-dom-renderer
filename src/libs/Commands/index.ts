import { DomCommand, IDomCommands, IDomCommandKey, IDomCommand, IDomCommandPayload, IDomCommandValue } from './DomCommand';

export const Commands = {
  DomCommand,
};

export type {
  IDomCommands, IDomCommandKey, IDomCommand, IDomCommandPayload, IDomCommandValue,
};
